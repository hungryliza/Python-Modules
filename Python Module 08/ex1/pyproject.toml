[tool.poetry]
name = "matrix_venv"
version = "0.1.0"
authors = ["limelo-c"]
package-mode = false

[tool.poetry.dependencies]
python = "^3.13"
pandas = "^3.0.3"
numpy= "^2.4.6"
matplotlib = "^3.10.9"
requests = "^2.34.2"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"